# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# HTTP clients
requests==2.31.0
httpx==0.26.0

# Vector Database & Embeddings
chromadb==0.4.22
sentence-transformers==2.3.1
numpy==1.26.3

# Data Processing
pandas==2.1.4
python-dateutil==2.8.2

# Logging
loguru==0.7.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# Utilities
python-multipart==0.0.6
jinja2==3.1.3

# Note: LLM provider calls are made via HTTP requests to the configured provider (Cerebras). Remove or add LLM SDKs
# (anthropic, openai, langchain, etc.) as needed depending on provider integrations in your deployment.